import { IBoard } from '../../data/IBoard';
import { IState } from './boardReducer';

export function writeBoardFromState(state: IState) {
    // TODO: saving logic
    return undefined as any as IBoard;
}